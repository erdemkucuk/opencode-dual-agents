networks:
  agents:
    driver: bridge

services:
  agent1:
    build: .
    environment:
      - AGENT_NAME=agent1
    volumes:
      - ./agent1-config:/agent
    ports:
      - "4097:4096"   # opencode serve
      - "4100:8000"   # custom FastMCP SSE server
    networks:
      - agents

  agent2:
    build: .
    environment:
      - AGENT_NAME=agent2
    volumes:
      - ./agent2-config:/agent
    ports:
      - "4098:4096"   # opencode serve
      - "4099:8000"   # custom FastMCP SSE server
    networks:
      - agents
